This folder is reserved for any screenshots or images used in the write-up
(e.g. before/after screenshots, or thumbnail frames from the demo video).

No images were needed for the pricing layout itself — it's built entirely
with CSS (borders, colors, spacing), no image assets required.
